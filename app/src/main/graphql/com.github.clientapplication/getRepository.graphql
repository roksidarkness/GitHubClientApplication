query GetRepositories {
    viewer {
        repositories(first: 100, ownerAffiliations: OWNER) {
            totalCount
            nodes {
                id
                name
                shortDescriptionHTML
                primaryLanguage{
                    name
                }
            }
            pageInfo {
                endCursor
                hasNextPage
            }
        }
    }
}